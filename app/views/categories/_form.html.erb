<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block", class: "col-sm-2 col-form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>
  <div>
    <%= form.label :details, style: "display: block",  class: "col-sm-2 col-form-label" %>
    <%= form.text_area :details, class: "form-floating form-control input-width" %>
    <br>
    <br>
  </div>
  <div>
    <%= form.submit class:"btn btn-success" %>
  </div>
<% end %>
